package com.vishu.tutorial;

import java.util.Locale;

public class tut14 {
    public static void main(String[] args) {
        String name="Vishu saroha";
        //System.out.println(name);
        //int value=name.length();//it is used for find the length of the string.
       // String value= name.toLowerCase();//it is used for convert all string into small letters
        //String value=name.toUpperCase();//it is used for convert all string into capital letters
       // String value=name.trim();
        //String value=name.substring(3);//it is used for remove the letter in the string.
        //String value=name.replace('s','g');//it is used for replace the alphabets of any other alphabets.
       // System.out.println(name.startsWith("Vi"));//it is used for boolean values only true and false.
        System.out.println(name.endsWith("ha"));//it ia also used for boolean values
        

    }
}
